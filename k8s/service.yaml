apiVersion: v1
kind: Service
metadata:
  name: saas-honeycomb-ordermgr
spec:
  selector:
    app: saas-honeycomb-ordermgr
  ports:
  - name: saas-honeycomb-ordermgr
    protocol: TCP
    port: 8091
    nodePort: 30091
  type: NodePort
